import styled from 'styled-components';

const StyledAvatarModal = styled.ul`
    background-color: white;
    list-style-type: none;
    width: 150px;
    position: absolute;
    right: 0;
`;

export default StyledAvatarModal;
