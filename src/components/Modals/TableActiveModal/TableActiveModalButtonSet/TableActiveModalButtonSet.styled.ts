import styled from 'styled-components';

export const StyledTableActiveModalButtonSet = styled.ul`
    position: absolute;
    display: flex;
    justify-content: flex-end;
    gap: 1rem;
    list-style: none;
    bottom: 2em;
    right: 2em;
`;
