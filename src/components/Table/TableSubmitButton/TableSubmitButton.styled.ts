import styled from 'styled-components';

export const TableSubmitButton = styled.button`
    border: none;

    width: 80%;
    height: 3rem;
    border-radius: 10em;
    cursor: pointer;
    :hover {
    }
`;
